spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: hoangson145juzk@gmail.com
    password: aqbq onja jbkd nmiu

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: "jdbc:mysql://localhost:3306/model_trade"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: ModelTradeAPI
server:
  port: 8080
  servlet:
    context-path: /model_trade


